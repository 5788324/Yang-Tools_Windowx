# 2026-05-26 工作日志

## 目标

- 继续工作流，降低后续 AI 接手成本。
- 将免安装打包流程从手工步骤固化为可重复运行的脚本。

## 已完成

- 确认仓库与远端同步，工作区只有被忽略的缓存、构建产物、插件样本和 release 包。
- 新增 portable 打包脚本：
  - `scripts/package-portable.ps1`
- 新增 npm 命令：
  - `npm.cmd run package:portable`
- 脚本会生成：
  - `release/Yang-Tools-0.1.0-portable`
  - `release/Yang-Tools-0.1.0-portable.zip`
- 打包脚本内容：
  - 复制 Electron runtime
  - 重命名 `electron.exe` 为 `Yang Tools.exe`
  - 复制 `out/` 构建产物
  - 复制最小运行依赖 `@electron-toolkit/utils`
  - 如果存在 `local-plugin-library`，复制到 `resources/local-plugin-library`
  - 生成 `README.txt`
- 修复脚本兼容性：
  - Windows PowerShell 5 对无 BOM UTF-8 中文脚本字符串解析不稳定
  - 脚本内 README 文案改为 ASCII 英文

## 修改文件

- `package.json`
- `scripts/package-portable.ps1`
- `docs/HANDOFF.md`
- `docs/worklogs/2026-05-26.md`

## 验证

以下命令已通过：

```powershell
npm.cmd run build
npm.cmd run typecheck
npm.cmd run package:portable
```

## 交接说明

- release 包仍然被 `.gitignore` 忽略，不提交到 GitHub。
- 如果用户测试 portable 版本失败，先重新运行：

  ```powershell
  npm.cmd run build
  npm.cmd run package:portable
  ```

- 如果 PowerShell 脚本报中文字符串解析错误，保持脚本 ASCII，不要在 `.ps1` 内写中文字符串。

# 2026-05-24 工作日志

## 目标

- 建立 Yang Tools 的基础工程和 AI 交接工作流。
- 分析本机 ZTools/uTools 插件样本，为后续适配做准备。
- 搭建类似 PowerToys/ZTools/uTools 的个人 Windows 桌面工具箱 MVP。

## 已完成

- 克隆并初始化仓库 `5788324/Yang-Tools_Windowx`。
- 读取用户已有开发文档和本机插件目录：
  - `C:\Users\YANG\AppData\Roaming\ZTools\plugins`
  - `C:\Users\YANG\AppData\Roaming\uTools\plugins`
- 将 ZTools/uTools 插件样本复制到 `local-plugin-library/`，并加入 `.gitignore`。
- 解析 6 个 ZTools 样本和 97 个 uTools remote 插件元信息。
- 创建 Electron + Vue + TypeScript 工程。
- 实现主窗口、托盘、`Alt+Space` 全局呼出、关闭隐藏到托盘。
- 实现插件样本扫描、插件列表 UI、插件窗口打开入口。
- 实现插件专用 preload 和基础兼容 API。
- 实现 ZTools 命令匹配和“运行匹配”入口。
- 将 `calculation-paper` 标记为 `api-ready`。
- 新增 ZTools API 使用分析脚本和兼容状态文档。
- 新增内存版文本剪贴板历史：
  - 主进程轮询系统剪贴板文本
  - IPC 暴露 `plugin:clipboard-search`
  - IPC 暴露 `plugin:clipboard-get-history`
  - preload 暴露 `window.ztools.clipboard.search`
  - preload 暴露 `window.ztools.clipboard.getHistory`
- 将 `clipboard` 从 `blocked` 调整为 `api-ready`，但只覆盖文本历史。
- 新增 Yang Tools 受管理插件目录和插件管理器：
  - `src/main/managedPlugins.ts`
  - 列出 `{userData}/plugins` 中已安装插件
  - 从本机 ZTools 样本安装插件
  - 更新时覆盖已安装插件
  - 卸载时只删除受管理插件目录内的插件
- 主界面改为“插件管理中心”：
  - 显示已安装插件
  - ZTools 样本支持安装/更新
  - 已安装插件支持打开/卸载
  - uTools 索引继续显示为待适配

## 修改文件

- `docs/HANDOFF.md`
- `docs/PLUGIN_COMPAT_API.md`
- `docs/PLUGIN_COMPAT_STATUS.md`
- `docs/PLUGIN_SPEC.md`
- `docs/worklogs/2026-05-24.md`
- `src/main/clipboardHistoryManager.ts`
- `src/main/index.ts`
- `src/main/managedPlugins.ts`
- `src/main/pluginIpc.ts`
- `src/main/pluginSamples.ts`
- `src/main/windowManager.ts`
- `src/preload/index.ts`
- `src/preload/plugin.ts`
- `src/renderer/src/App.vue`
- `src/renderer/src/styles.css`
- `src/shared/pluginTypes.ts`

历史上本日还新增/修改了工程脚手架、插件扫描、命令匹配、窗口管理、样式和分析脚本，详见 Git 提交记录。

## 验证

以下命令已通过：

```powershell
npm.cmd run typecheck
npm.cmd run build
npm.cmd run lint:manifests
```

插件管理中心改造后再次通过同一组命令。

## 问题与风险

- 当前还没有做 GUI 人工验收；`calculation-paper` 和 `clipboard` 是 API 层 ready，不代表完整体验已确认。
- 剪贴板历史目前只支持文本且仅内存保存。
- 不执行第三方插件 preload，因此依赖自带 preload 注册工具的插件还需要后续兼容策略。
- 图片、文件、截图、OCR、翻译、插件安装/卸载/更新仍是后续任务。
- 远程下载、版本源、哈希校验还没有实现；当前只是本机样本安装/更新/卸载。

## 下一步

- 启动 `npm.cmd run dev` 人工验证 `calculation-paper` 和 `clipboard`。
- 人工验证插件安装、更新、卸载、打开流程。
- 设计插件权限清单和运行确认弹窗。
- 开始自研截图/钉图 MVP，避免直接依赖 ZTools 的高权限截图插件。
